{{- $fields := slice "text_markdown" "state" -}}
{
  label: '{{ i18n "admin.files.banner" }}',
  name: 'banner',
  file: 'data/{{ .env }}/banner.yml',
  i18n: true,
  {{- partial "admin/fields/_fields.yml" $fields -}}
}